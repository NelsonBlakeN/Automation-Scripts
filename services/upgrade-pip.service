[Unit]
Description=Upgrade the pip version for each virtual environment stored in the home folder

[Service]
Environment=SCRIPTS=/home/blake/scripts/Automation
Environment=LOGS=/home/blake/Documents/logs
ExecStart=/bin/bash -c "${SCRIPTS}/upgrade_pip.sh | tee -a ${LOGS}/upgrade_pip.log ${LOGS}/GlobalLog"

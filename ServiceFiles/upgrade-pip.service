[Unit]
Description=Upgrade the pip version for each virtual environment stored in the home folder

[Service]
Environment=SCRIPTS=/home/blake/scripts/autoscripts
ExecStart=/bin/bash -c "${SCRIPTS}/upgrade_pip"
